import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: 'black',
    },

    statusbar: {
        backgroundColor: '#61dafb',
        barStyl: "dark-content",
    },

    imagens: {
        marginTop: -100,
        height: 600,
        backgroundImage: 'cover',
    },

    Text: {
        justifyContent: 'center',
        textAlign: "center", 
        color: '#fff',  
        marginTop: 40,
        fontSize: 20,
    },
  });